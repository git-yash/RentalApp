import {StyleSheet} from 'react-native';

const finishSigningUpStyle = StyleSheet.create({
  termsAndConditionTextStyle: {
    fontFamily: 'Poppins-Regular',
    margin: 15,
    fontSize: 12,
  },
});

export default finishSigningUpStyle;
